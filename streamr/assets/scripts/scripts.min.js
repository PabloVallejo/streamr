
(function() {
  var list, math, number, numebr, opposite, square;

  numebr = 43;

  opposite = true;

  if (opposite) {
    number = -42;
  }

  square = function(x) {
    return x * x;
  };

  list = [1, 2, 3, 4];

  math = {
    root: Math.sqrt,
    square: square,
    cube: function(x) {
      return x * square(x);
    }
  };

  console.log('Hello Coffee script');

}).call(this);
